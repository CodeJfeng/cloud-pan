<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeng.pan.server.modules.file.mapper.RPanUserFileMapper">

    <resultMap id="BaseResultMap" type="com.jfeng.pan.server.modules.file.entity.RPanUserFile">
            <id property="fileId" column="file_id" />
            <result property="userId" column="user_id" />
            <result property="parentId" column="parent_id" />
            <result property="realFileId" column="real_file_id" />
            <result property="filename" column="filename" />
            <result property="folderFlag" column="folder_flag" />
            <result property="fileSizeDesc" column="file_size_desc" />
            <result property="fileType" column="file_type" />
            <result property="delFlag" column="del_flag" />
            <result property="createUser" column="create_user" />
            <result property="createTime" column="create_time" />
            <result property="updateUser" column="update_user" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        file_id,user_id,parent_id,real_file_id,filename,folder_flag,
        file_size_desc,file_type,del_flag,create_user,create_time,
        update_user,update_time
    </sql>

    <select id="selectFileList" resultType="com.jfeng.pan.server.modules.file.vo.RPanUserFileVO">
        SELECT
            file_id AS fileId,
            parent_id AS parentId,
            filename AS fileName,
            file_size_desc AS fileSizeDesc,
            folder_flag AS folderFlag,
            file_type AS fileType,
            update_time AS updateTime
        FROM
            r_pan_user_file
        WHERE
            user_id = #{param.userId}
        <if test="param.fileIdList != null">
            AND file_id IN
            <foreach collection="param.fileIdList" open="(" close=")" item="item" separator=",">
                #{item}
            </foreach>
        </if>
        <if test="param.parentId != null and param.parentId != -1">
            AND parent_id = #{param.parentId}
        </if>
        <if test="param.fileTypeArray != null ">
            AND file_type IN
            <foreach collection="param.fileTypeArray" open ="(" close = ")" item="item" separator=",">
                #{item}
            </foreach>
        </if>
        AND del_flag = #{param.delFlag}
    </select>


    <select id="searchFile" resultType="com.jfeng.pan.server.modules.file.vo.FileSearchResultVO">
        SELECT
        file_id AS fileId,
        parent_id AS parentId,
        filename AS fileName,
        file_size_desc AS fileSizeDesc,
        folder_flag AS folderFlag,
        file_type AS fileType,
        update_time AS updateTime
        FROM
        r_pan_user_file
        WHERE
        user_id = #{param.userId}
        AND filename LIKE CONCAT(#{param.keyword}, '%')
        <if test="param.fileTypeArray != null ">
            AND file_type IN
            <foreach collection="param.fileTypeArray" open ="(" close = ")" item="item" separator=",">
                #{item}
            </foreach>
        </if>
        AND del_flag = #{param.delFlag}
    </select>


</mapper>
